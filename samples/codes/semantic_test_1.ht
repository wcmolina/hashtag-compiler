begin
    function int suma(int a, int b) do
        return a+b;
    end

    mainbegin do
        int x=readint();
        int y=readint();
        
        if (x<y) do
            int g = x+y+10;
            int h = g/100;
        end
        int h = suma(4,4);
        int i = suma("hola",10);
        int j = suma();
        int k = suma(h,suma(5,notdeclared));
        int l = suma(4.6,true);
    end
end