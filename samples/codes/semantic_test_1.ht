begin
	function int suma() do
		int x;
		int y;
	end

	mainbegin do
		int x;
		int y;
		if (a<b) do
			int g = 0;
			int h;
		end
		h = 0; # error, h se declaro en el scope del if, solo ahi se puede ver. lo mismo con g
	end
end