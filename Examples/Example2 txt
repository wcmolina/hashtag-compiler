begin

int i = 0;
        while (true)
 {Condición: siempre cierta}
            i++;
            print ("Valor de i: " + i);
            if (i==9) begin 
            break; endif
                      

public int sumar(int a, int b)begin
           int c;
           c = a + b;
           return c;
    end
{Probando  
   boolean}
#comentario de una linea
public boolean esBisiesto(int a)begin   
        if(a%4==0 && a%100!=0 or a%400==0)
           return true;
        else
           return false;
    end

end
