begin
    function int suma(int a, int b) do
        return a + b;
    end

    function string saludo () do
        return "Hola mundo";
    end

    mainbegin do
        if (id < b) do
            if (id2 < h) do
                if (c < j) do
                    if (k < q) do
                    end
                end
            end
        end

        if (var < id4) do
            if (id4 < var2) do
                if (v < b) do
                    if (n < v) do
                    end
                end
            end
        end

        if (foo < bar) do
            int declare_me = 8;
            int b;
        end else do
            int inside_else_body = 9;
        end

        if (label == "cadena") do
            if (id4 < var2) do
                if (g < f) do
                    int w = 1;
                    if (n < v) do
                        string v = "hola";
                    end
                end
            end
        end

        for (int a = 1; a < 7; a++) do
            for (int c = 10; c > 0; a--) do
                boolean b = true;
                for (int z = 1; z < 45; z++) do
                    if (bar == "cadena") do
                        if (id4 < var2) do
                            if (g < f) do
                                int w = 1;
                                if (n < v) do
                                    string v = "hola";
                                end
                            end
                        end
                    end
                end
            end
        end

        if (var < id4) do
            if (id4 < var2) do
                if (v < b) do
                    if (n < v) do
                        int aritmetica = 7+6+9*8/5+7+8;
                    end
                end
            end
        end

        if (var < id4) do
            if (id4 < var2) do
                if (v < b) do
                    if (n < v) do
                    end
                end
            end
        end

        int contador = 0;
    end
end